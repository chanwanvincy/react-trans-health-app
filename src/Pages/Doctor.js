import { useState } from "react";

const DoctorPage = ({ doctors, id, currentDoctor }) => {
    // console.log(currentDoctor)
    return (
        // <DefaultLayout>
        <h1>Doctor Page</h1>
        // </DefaultLayout>
    );
};

export default DoctorPage